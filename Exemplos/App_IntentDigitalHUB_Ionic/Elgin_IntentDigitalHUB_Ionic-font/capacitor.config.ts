import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.elgin.ionic.intent_digital_hub",
  appName: "Ionic_IntentDigitalHub",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
